<form method="post" action="{{ path('factura_detalle_delete', {'id': factura_detalle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ factura_detalle.id) }}">
    <button class="btn">Delete</button>
</form>
