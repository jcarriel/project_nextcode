{% extends 'base.html.twig' %}

{% block title %}Configuracion{% endblock %}

{% block body %}
    <h1>Configuracion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ configuracion.id }}</td>
            </tr>
            <tr>
                <th>ESTABLECIMIENTO</th>
                <td>{{ configuracion.establecimiento }}</td>
            </tr>
            <tr>
                <th>PUNTO EMISION</th>
                <td>{{ configuracion.puntoEmision }}</td>
            </tr>
            <tr>
                <th>SEC FACTURA</th>
                <td>{{ configuracion.secFactura }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('configuracion_index') }}" class="btn btn-info btn-sm">VOLVER A LA LISTA</a>

    <a href="{{ path('configuracion_edit', {'id': configuracion.id}) }}" class="btn btn-warning btn-sm">EDITAR</a>

    {{ include('configuracion/_delete_form.html.twig') }}
{% endblock %}
